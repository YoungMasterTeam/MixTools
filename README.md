# MixTools
YoungMasterTeam!

Need install
$ pkg install git
$ pkg install figlet
$ pkg install ruby
$ gem install lolcat
$ apt install toilet
$ git clone https://github.com/YoungMasterTeam/MixTools
$ cd MixTools
$ sh Tools.sh

Password : CodersGans
